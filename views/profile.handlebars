<form class="create-form">
    <div class="form-group align-self-center">
        <div class="container">
            <div class="row justify-content-center">
                <div class="col-sm-5">
                    <h4 class="header">Profile</h4>

                    <ul class="username">
                        username:
                        {{users.username}}
                    </ul>
                    <ul class="useremail">
                        email:
                        {{users.email}}
                    </ul>
                    <ul class="userskill">
                        Skill Level:
                        {{users.skillLevel}}
                    </ul>
                </div>
                <button type="submit" class="fnButton">Save</button>
            </div>
        </div>
    </div>
</form>